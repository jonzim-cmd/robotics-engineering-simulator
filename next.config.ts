import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/robotics-engineering-simulator', // Assuming the repository name is 'robotics-engineering-simulator'
};

export default nextConfig;
